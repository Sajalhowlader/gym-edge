import React from 'react';
import Header from '../Sheared/Header/Header';

const Contacts = () => {
    return (
        <div>
            <Header color="black" />
        </div>
    );
};

export default Contacts;